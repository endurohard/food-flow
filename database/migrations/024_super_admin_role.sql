-- ============================================================
-- Миграция 024: платформенная роль super_admin
-- Супер-админ не привязан к предприятию: создаёт организации и их
-- владельцев. Роль недоступна через публичный /register — только сид
-- (services/user-service/scripts/create-superadmin.js).
-- ============================================================

ALTER TYPE user_role ADD VALUE IF NOT EXISTS 'super_admin';
