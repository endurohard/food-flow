# FoodFlow - Roadmap функционала

Анализ на основе FusionPOS и планирование развития проекта.

## Текущий функционал ✅

### Уже реализовано:
- ✅ Микросервисная архитектура (User, Restaurant, Order, Delivery, Kitchen Services)
- ✅ API Gateway (Kong)
- ✅ Система заказов
- ✅ Служба доставки
- ✅ KDS (Kitchen Display System)
- ✅ Управление принтерами
- ✅ Управление столами
- ✅ Управление персоналом
- ✅ Складской учет (базовый)
- ✅ Управление меню с категориями
- ✅ Модификаторы блюд
- ✅ Техкарты (рецепты)
- ✅ Расчет себестоимости блюд
- ✅ Ценовые схемы
- ✅ Стоп-листы
- ✅ Программа лояльности (базовая)
- ✅ Аналитика (базовая)
- ✅ Telegram Bot для обработки накладных
- ✅ OCR для распознавания накладных
- ✅ Минималистичный UI в стиле iiko

---

## Приоритетные функции для добавления 🎯

### 1. Работа с залами и столами (Высокий приоритет)

**Статус:** Частично реализовано

**Что нужно добавить:**
- [ ] Визуальная схема зала с перетаскиванием столов
- [ ] Статусы столов (свободен, занят, резерв, требует уборки)
- [ ] Перенос заказа между столами
- [ ] Объединение столов
- [ ] Разделение счета по гостям
- [ ] Разделение счета по позициям
- [ ] Таймер занятости стола
- [ ] Цветовая индикация состояния столов

**Технические задачи:**
```typescript
// Новые модели
interface TableLayout {
  id: number;
  hallId: number;
  tables: Table[];
  layout: {
    table: number;
    x: number;
    y: number;
    rotation: number;
  }[];
}

interface TableStatus {
  id: number;
  status: 'free' | 'occupied' | 'reserved' | 'cleaning';
  guestCount: number;
  occupiedSince?: Date;
  waiter?: number;
  currentOrderId?: number;
}

// API endpoints
POST /api/tables/{id}/transfer-order
POST /api/tables/merge
POST /api/orders/{id}/split-bill
```

---

### 2. Режимы работы заведения (Высокий приоритет)

**Что добавить:**
- [ ] Режим "Кафе/Ресторан" (обслуживание по столам)
- [ ] Режим "Фаст-фуд" (заказы без привязки к столу)
- [ ] Режим "Фудтрак" (только на вынос)
- [ ] Режим "Доставка" (только доставка)
- [ ] Гибридный режим (комбинация нескольких)
- [ ] Переключение режимов по времени (автоматически)

**Реализация:**
```typescript
enum ServiceMode {
  DINE_IN = 'dine_in',           // Столы
  FAST_FOOD = 'fast_food',       // Быстрое обслуживание
  FOOD_TRUCK = 'food_truck',     // Фудтрак
  DELIVERY_ONLY = 'delivery_only', // Только доставка
  HYBRID = 'hybrid'              // Комбинированный
}

interface ModeSchedule {
  dayOfWeek: number;
  mode: ServiceMode;
  startTime: string;
  endTime: string;
}
```

---

### 3. Расширенный складской учет (Высокий приоритет)

**Что добавить:**
- [ ] Метод учета FIFO (First In, First Out)
- [ ] Производство полуфабрикатов
- [ ] Автоматическое списание по техкартам
- [ ] Инвентаризация с мобильного приложения
- [ ] Критические остатки с уведомлениями
- [ ] Перемещение между складами
- [ ] Партионный учет с датами истечения
- [ ] Калькуляция себестоимости с учетом FIFO

**Модели данных:**
```typescript
interface InventoryBatch {
  id: number;
  itemId: number;
  quantity: number;
  costPrice: number;
  arrivalDate: Date;
  expirationDate?: Date;
  supplierInvoiceId: string;
  warehouseId: number;
}

interface Production {
  id: number;
  recipeId: number;
  quantity: number;
  producedAt: Date;
  usedIngredients: {
    batchId: number;
    quantity: number;
  }[];
  producedBatch: InventoryBatch;
}

interface InventoryTransaction {
  id: number;
  type: 'arrival' | 'writeoff' | 'transfer' | 'production' | 'inventory';
  itemId: number;
  batchId?: number;
  quantity: number;
  fromWarehouse?: number;
  toWarehouse?: number;
  reason: string;
  userId: number;
  createdAt: Date;
}
```

---

### 4. CRM и программа лояльности (Средний приоритет)

**Что добавить:**
- [ ] База клиентов с историей заказов
- [ ] Группы клиентов (VIP, Regular, New)
- [ ] Накопительные скидки
- [ ] Бонусная система (кэшбэк)
- [ ] Счастливые часы (Happy Hours)
- [ ] Персональные предложения
- [ ] SMS/Email рассылки
- [ ] Push-уведомления
- [ ] Интеграция с UDS Game / MyBonus

**Структура:**
```typescript
interface Customer {
  id: number;
  phone: string;
  email?: string;
  name?: string;
  birthdate?: Date;
  group: 'new' | 'regular' | 'vip';
  totalSpent: number;
  totalOrders: number;
  bonusPoints: number;
  discount: number; // Накопительная скидка
  registeredAt: Date;
  lastOrderAt?: Date;
  tags: string[];
}

interface LoyaltyProgram {
  id: number;
  name: string;
  type: 'discount' | 'bonus' | 'cashback';
  conditions: {
    minOrderAmount?: number;
    customerGroup?: string[];
    daysOfWeek?: number[];
    timeRange?: { start: string; end: string };
  };
  reward: {
    discountPercent?: number;
    bonusPercent?: number;
    fixedDiscount?: number;
  };
}

interface HappyHour {
  id: number;
  name: string;
  daysOfWeek: number[];
  startTime: string;
  endTime: string;
  discountPercent: number;
  applicableCategories?: number[];
  applicableItems?: number[];
}
```

---

### 5. Бронирование столов (Средний приоритет)

**Что добавить:**
- [ ] Онлайн-бронирование через веб-форму
- [ ] Бронирование через Telegram Bot
- [ ] Календарь бронирований
- [ ] Депозит за бронирование
- [ ] Автоматические напоминания
- [ ] Блэклист гостей (no-show)
- [ ] Предзаказ блюд при бронировании

**API:**
```typescript
interface Reservation {
  id: number;
  customerId: number;
  tableId: number;
  date: Date;
  time: string;
  guestCount: number;
  duration: number; // минуты
  status: 'pending' | 'confirmed' | 'seated' | 'completed' | 'cancelled' | 'noshow';
  depositAmount?: number;
  depositPaid?: boolean;
  preOrder?: {
    dishId: number;
    quantity: number;
  }[];
  notes?: string;
  createdAt: Date;
}

// Endpoints
POST /api/reservations
GET /api/reservations/calendar
PUT /api/reservations/{id}/confirm
PUT /api/reservations/{id}/cancel
POST /api/reservations/{id}/seat
```

---

### 6. Расширенная аналитика и отчеты (Средний приоритет)

**Что добавить:**
- [ ] Дашборд владельца (FusionBoard-аналог)
- [ ] Продуктивность официантов
- [ ] ABC-анализ блюд
- [ ] Анализ рентабельности блюд
- [ ] Отчет по отходам и потерям
- [ ] Прогноз продаж
- [ ] Анализ пиковых часов
- [ ] Средний чек по часам/дням
- [ ] Конверсия гостей в постоянных клиентов
- [ ] Отчет по источникам заказов

**Отчеты:**
```typescript
interface WaiterPerformance {
  waiterId: number;
  waiterName: string;
  ordersCount: number;
  totalRevenue: number;
  averageCheck: number;
  averageServiceTime: number; // минуты
  rating: number;
  tips: number;
}

interface DishAnalytics {
  dishId: number;
  dishName: string;
  category: 'A' | 'B' | 'C'; // ABC-анализ
  soldCount: number;
  revenue: number;
  costPrice: number;
  profit: number;
  profitMargin: number; // %
  popularity: number; // % от всех заказов
}

interface PeakHoursAnalysis {
  hour: number;
  ordersCount: number;
  revenue: number;
  averageCheck: number;
  guestsCount: number;
  averageWaitTime: number;
}
```

---

### 7. Мобильное приложение для официантов (Высокий приоритет)

**Что добавить:**
- [ ] React Native / Flutter приложение
- [ ] Принятие заказов на планшете
- [ ] Просмотр меню с фото
- [ ] Добавление модификаторов
- [ ] Отправка заказа на кухню
- [ ] Статус приготовления блюд
- [ ] Печать чека
- [ ] Прием оплаты
- [ ] Офлайн-режим с синхронизацией

**Экраны:**
1. Список столов
2. Меню с категориями
3. Корзина заказа
4. История заказов
5. Профиль официанта

---

### 8. Приложение директора / владельца (Средний приоритет)

**Что добавить:**
- [ ] Мобильный дашборд (iOS/Android)
- [ ] Просмотр выручки в реальном времени
- [ ] Количество гостей
- [ ] Остатки на складе
- [ ] Критические события (стоп-лист, нехватка продуктов)
- [ ] Push-уведомления о проблемах
- [ ] Удаленное управление меню
- [ ] Просмотр камер (интеграция)
- [ ] Финансовые отчеты

---

### 9. Маршрутизация заказов (Высокий приоритет)

**Что добавить:**
- [ ] Автоматическая отправка блюд на разные кухни
- [ ] Горячий цех
- [ ] Холодный цех
- [ ] Бар
- [ [ ] Кондитерский цех
- [ ] Приоритеты приготовления
- [ ] Синхронизация времени подачи (fire button)

**Реализация:**
```typescript
interface KitchenStation {
  id: number;
  name: string;
  type: 'hot' | 'cold' | 'bar' | 'pastry' | 'grill';
  printerIds: number[];
  displayIds: number[];
  categories: number[]; // Какие категории блюд готовятся здесь
}

interface OrderRouting {
  orderId: number;
  items: {
    itemId: number;
    kitchenStation: number;
    priority: number;
    estimatedTime: number;
    fireTime?: Date; // Когда начать готовить (для синхронизации)
  }[];
}
```

---

### 10. Интеграции (Средний приоритет)

**Что добавить:**
- [ ] Интеграция с Яндекс.Еда API
- [ ] Интеграция с Delivery Club API
- [ ] Интеграция с 2GIS
- [ ] Интеграция с Google Maps / Яндекс.Карты
- [ ] Интеграция с онлайн-кассами (54-ФЗ)
- [ ] Интеграция с ЕГАИС (для алкоголя)
- [ ] Интеграция с банковскими терминалами
- [ ] Интеграция с СМС-шлюзами
- [ ] Интеграция с email-сервисами
- [ ] Webhooks для сторонних систем

---

### 11. Расчет зарплаты и мотивация (Низкий приоритет)

**Что добавить:**
- [ ] Учет рабочего времени
- [ ] Расчет зарплаты по ставке/часам
- [ ] Процент от продаж
- [ ] KPI для персонала
- [ ] Бонусы за выполнение планов
- [ ] Штрафы за ошибки
- [ ] Табель учета рабочего времени
- [ ] Интеграция с 1С:ЗУП

---

### 12. Система скидок и промокодов (Средний приоритет)

**Что добавить:**
- [ ] Промокоды на скидку
- [ ] Промокоды на бесплатную доставку
- [ ] Купоны на бесплатное блюдо
- [ ] Скидки по дням недели
- [ ] Скидки для групп клиентов
- [ ] Комбинированные скидки
- [ ] Ограничения по использованию
- [ ] Автоматическое применение лучшей скидки

```typescript
interface Promocode {
  code: string;
  type: 'discount' | 'free_delivery' | 'free_item' | 'fixed_amount';
  value: number; // процент или сумма
  minOrderAmount?: number;
  maxDiscount?: number;
  validFrom: Date;
  validTo: Date;
  usageLimit?: number;
  usedCount: number;
  applicableItems?: number[];
  applicableCategories?: number[];
  customerGroups?: string[];
}
```

---

### 13. Уведомления и коммуникации (Средний приоритет)

**Что добавить:**
- [ ] SMS-уведомления о статусе заказа
- [ ] Email-уведомления
- [ ] Push-уведомления в приложении
- [ ] Telegram-уведомления
- [ ] Уведомления персонала о новых заказах
- [ ] Уведомления кухни о срочных заказах
- [ ] Уведомления директору о проблемах

---

### 14. Онлайн-заказ для гостей (Высокий приоритет)

**Что добавить:**
- [ ] Веб-приложение для заказа
- [ ] Мобильное приложение для гостей
- [ ] Корзина и оформление заказа
- [ ] Онлайн-оплата (Stripe, ЮKassa)
- [ ] Отслеживание статуса заказа
- [ ] История заказов
- [ ] Избранные блюда
- [ ] Рекомендации на основе истории

---

### 15. Дополнительные функции

**Управление контентом:**
- [ ] Фотографии блюд
- [ ] Описания и состав
- [ ] Аллергены
- [ ] Калорийность и БЖУ
- [ ] Видео-рецепты для гостей

**Безопасность:**
- [ ] Двухфакторная аутентификация
- [ ] Аудит действий пользователей
- [ ] Резервное копирование данных
- [ ] Шифрование чувствительных данных

**Мультиязычность:**
- [ ] Русский
- [ ] Английский
- [ ] Другие языки меню

**Франшиза / Сеть:**
- [ ] Управление несколькими заведениями
- [ ] Консолидированная отчетность
- [ ] Центральное управление меню
- [ ] Трансфер между заведениями

---

## План внедрения (3 месяца)

### Месяц 1: Операционная эффективность
1. ✅ Работа с залами и столами
2. ✅ Режимы работы заведения
3. ✅ Маршрутизация заказов на кухни
4. ✅ Мобильное приложение для официантов (MVP)

### Месяц 2: Складской учет и аналитика
1. ✅ FIFO и партионный учет
2. ✅ Производство полуфабрикатов
3. ✅ Расширенная аналитика
4. ✅ Дашборд директора

### Месяц 3: Клиентский опыт
1. ✅ CRM и программа лояльности
2. ✅ Бронирование столов
3. ✅ Онлайн-заказ для гостей
4. ✅ Система промокодов и скидок

---

## Технический стек для новых функций

### Frontend (Mobile Apps)
- **React Native** или **Flutter** для кроссплатформенных приложений
- **Redux / MobX** для state management
- **React Navigation** для навигации
- **Axios** для API запросов

### Backend (New Services)
- **CRM Service** (Node.js + TypeScript + PostgreSQL)
- **Reservation Service** (Node.js + TypeScript + PostgreSQL)
- **Analytics Service** (Node.js + ClickHouse для больших данных)
- **Notification Service** (Node.js + Redis + Bull для очередей)

### Инфраструктура
- **ClickHouse** для аналитики и отчетов
- **Redis Pub/Sub** для реалтайм-уведомлений
- **WebSocket Server** для live updates
- **MinIO** для хранения файлов (фото блюд, накладные)
- **Grafana** для мониторинга бизнес-метрик

---

## Выводы

FusionPOS - это комплексное решение, которое охватывает все аспекты ресторанного бизнеса. Основные направления для развития нашего FoodFlow:

**Критически важно:**
1. Визуальная работа с залами и столами
2. Мобильное приложение для официантов
3. Онлайн-заказ для гостей
4. Маршрутизация на разные кухни

**Важно для масштабирования:**
1. CRM и лояльность
2. Расширенная аналитика
3. Бронирование столов
4. Складской учет с FIFO

**Nice to have:**
1. Интеграции с агрегаторами
2. Приложение директора
3. Мультиязычность
4. Франшизная модель

Наш проект уже имеет хорошую базу, и добавление этих функций сделает его конкурентоспособным решением на рынке.
